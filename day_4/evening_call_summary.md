# Evening call summary — Day 4

**Date:** 2026-05-08

During the evening call, Liul confirmed the explainer landed both halves of the question on a single mechanism — *centroid is a first-moment summary, every property of the cohort beyond the mean is silent* — and that this binding made the n_eff-vs-n_reported correction and the "semantic stability" evidence chain feel like two rungs of the same ladder rather than two separate fixes. The runnable demo at [`day_4/scripts/centroid_drift_demo.py`](scripts/centroid_drift_demo.py) was load-bearing: with cohorts mean-matched by construction, `drift_score = 9.78e-13` (machine zero) and `dispersion ratio = 9.99x` made the failure mode visible in one screen, and the permutation-test `p = 1.0` on the same statistic showed that even a test in the centroid family is blind by construction. The four mechanisms named in the explainer (genuine stability, dispersion-only shift, multimodal redistribution, fallback contamination) gave Liul a checklist for what `drift_score = 0.0` could otherwise mean. Liul signed off the gap as **Closed**, with the file-level rewrites carried into the grounding commit on [`Heban-7/Data-Contract-Enforcer`](https://github.com/Heban-7/Data-Contract-Enforcer): `contracts/ai_extensions.py` will emit a triple (`drift_score`, `dispersion_ratio`, `mmd_score`) plus provenance fields (`n_effective`, `n_reported_input`, `embedding_model_id`, `fallback_rate`); `report_final_pdf_ready.md` will replace the bare "Text content is semantically stable" sentence with the n_eff- and provenance-aware paragraph from the explainer's "What to actually change" section.

On the reverse direction, Liul confirmed the v1 explainer for my kappa-paradox question landed the `p_o` − `p_e` decomposition at one confusion matrix, named the paradox at concentrated marginals (Feinstein & Cicchetti 1990; Byrt et al. 1993), and gave a defended numerical range on my actual `inter_rater_agreement.json` marginals: under `Pr(rating=5) ≈ 0.63`, `Pr(rating=4) ≈ 0.34`, `Pr(rating=3) ≈ 0.03`, raw `agree_within_1 = 1.00` is consistent with **Cohen's kappa in the 0.0–0.3 range** (paradox-collapsed), **linear-weighted kappa in the 0.4–0.6 range** (the ordinal weighting partially rescues it), and **Gwet's AC1 in the 0.7–0.85 range** (paradox-resistant by construction). The recommendation: **report the triple in `inter_rater_agreement.md` and lead with Gwet's AC1 in `memo/memo.md`** — Gwet's expected-agreement term `1/(k-1)` does not collapse at concentrated marginals, so it's the honest summary of what my IRR exercise actually demonstrates. I signed off the gap as **Closed**, with one residual logged as a Day-5 candidate cohort topic: the bootstrap CI on AC1 at n=30 was deliberately scoped out, and the published memo would benefit from a CI alongside the point estimate before the final submission. The grounding commit lands three edits to `eyorata/sales_evaluation_bench`: a `kappa_and_ac1` block added to `inter_rater_agreement.json` reporting all three statistics, a rewritten certification sentence in `inter_rater_agreement.md` reporting the triple instead of the raw-agreement-only number, and a one-sentence update to the memo's "fit for purpose" paragraph that cites Gwet's AC1 as the headline IRR.
