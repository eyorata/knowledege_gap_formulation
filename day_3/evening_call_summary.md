# Evening call summary — Day 3

**Date:** 2026-05-07

During the evening call, Ruth confirmed her explainer-v1 had landed the KL-anchor mechanism in prose but had not given the gradient derivations I'd asked for, had conflated CPO with ORPO, and had no runnable demonstration or canonical citations. She delivered explainer-v2 in response, which (a) wrote out ∇_θ ℒ for DPO and SimPO at one preference pair and showed where π_ref enters DPO's `z` and disappears in SimPO's `z`, (b) explicitly corrected the CPO-vs-ORPO conflation — `cpo_alpha` controls CPO's additive NLL term (Xu et al. 2024), not ORPO's odds-ratio loss (Hong, Lee, Thorne 2024), (c) added a runnable numpy demo computing DPO and SimPO margins side-by-side, (d) cited the four canonical sources (Rafailov, Meng, Hong, TRL `CPOTrainer`). I signed off the gap as **Closed**, with one residual logged as a Day-4 candidate cohort topic: ORPO's odds-ratio gradient was deliberately scoped out of the demo, and a strict reading of `question.md`'s "satisfying answer" still wants the three-way numpy gradient triple. The grounding commit lands three edits to `eyorata/sales_evaluation_bench`: an honest rewrite of `methodology_rationale.md §3` that names the KL-anchor cost at gradient level, a CPO-vs-ORPO disambiguation note in the model card limitations section, and a queued `cpo_alpha=0.5` ablation entry with the explainer's directional prediction (Delta B → −0.15 to 0.0) and an explicit falsification condition.

On the reverse direction, Addisu confirmed my explainer for the LoRA rank question closed the gap fully on the first pass — the intrinsic-low-rank hypothesis named the single mechanism behind both observations (small adapters working + larger ranks not helping), and the `lora_rank_demo.py` SVD-spectrum table at r ∈ {2, 4, 16} gave Addisu a verifiable artifact to re-run with different `TRUE_RANK` values for self-testing the intuition. The published version of the explainer is at https://dev.to/eyorata/-what-lora-actually-adapts-and-why-higher-rank-doesnt-always-buy-what-it-looks-like-it-should-4bfp. Addisu's grounding commit on the Conversion Engine repo will pin the rank choice to `r=8` (intrinsic-rank-matched per the SVD audit) and add a "what `r` actually controls" paragraph to the training-config rationale.
